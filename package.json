{
  "name": "bicc-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix apps/public-site && npm install --prefix apps/admin-portal",
    "dev:public": "npm run dev --prefix apps/public-site",
    "dev:admin": "npm run dev --prefix apps/admin-portal",
    "build:public": "npm run build --prefix apps/public-site",
    "build:admin": "npm run build --prefix apps/admin-portal",
    "build": "npm run build:public && npm run build:admin"
  },
  "engines": {
    "node": "20.x"
  }
}
