# Deprecated — do not use

This `src/` folder is a **legacy copy** from before the project was split into:

- `apps/public-site` — public website
- `apps/admin-portal` — admin portal

All active development happens in those app folders. This code is not deployed and may be out of date.

Safe to delete once you confirm you are only using the `apps/` structure.
