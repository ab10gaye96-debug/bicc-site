// This file is kept for backward compatibility.
// All data operations now go through src/api.ts → Firebase Firestore.

export function initializeData() {
  // No-op: Firebase Firestore handles data persistence
}